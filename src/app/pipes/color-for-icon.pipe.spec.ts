import { ColorForIconPipe } from './color-for-icon.pipe';

describe('ColorForIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorForIconPipe();
    expect(pipe).toBeTruthy();
  });
});
