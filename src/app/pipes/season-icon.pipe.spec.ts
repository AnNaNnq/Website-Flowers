import { SeasonIconPipe } from './season-icon.pipe';

describe('SeasonIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonIconPipe();
    expect(pipe).toBeTruthy();
  });
});
